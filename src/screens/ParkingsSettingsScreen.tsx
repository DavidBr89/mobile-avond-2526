import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ParkingsSettingsScreen = () => {
  return (
    <View>
      <Text>ParkingsSettingsScreen</Text>
    </View>
  );
};

export default ParkingsSettingsScreen;

const styles = StyleSheet.create({});
